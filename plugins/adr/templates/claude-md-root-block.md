## Architecture decisions

Decisions that are expensive to undo are recorded in `{{DIR}}/`.
The rules are in [`{{DIR}}/CLAUDE.md`]({{DIR}}/CLAUDE.md).

Read `{{REGISTER}}` before you change an architectural boundary. Open only the ADRs that apply.
When you make a decision that is expensive to undo, write an ADR with `status: proposed`.
Do not stop to ask.
