# ADR-0001: Storage format and location

**Status:** accepted

The `bm` CLI stores bookmarks as newline-delimited JSON at `~/.bm/bookmarks.jsonl`.
Single user, local machine; no sync, no server, no auth.
